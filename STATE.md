# State

Authoritative snapshot of where Black Gold is. Update at every phase boundary and whenever the authoritative branch or approval status changes.

**Last updated:** 2026-09-21 by Claude Code. **PR #94 (D-51 step 2) is MERGED as `17c787b`, and PR #95 (test-suite headroom) as `cfef173`.** One caveat a fresh session must not miss: #94 was merged while a Codex review round was still in flight, so **two accepted P1 fixes did not make it into `main`** and are carried by a follow-up PR instead — the second §16.1 prong consuming the same gap-distorted NAV the first prong withholds on, and the gap scan covering sessions on which the position was flat. Until that follow-up merges, `main` under-withholds in one direction and over-withholds in the other. Everything below this paragraph was written against #94 and remains accurate for it.

**Prior entry (2026-09-20) — the primary metric was the wrong statistic.** Codex review of PR #94 found that `net_sharpe_difference_vs_primary_benchmark` was computed as `annualizedSharpe(candidate − benchmark)`, which is an **information ratio**; §13 registers a **difference of Sharpe ratios**. The repository said so itself — `informationRatio()` in `benchmarks.ts` is that same construction, and `armMetrics` published the number correctly named right beside the gate. **Every primary-metric number this project has recorded, including the −0.19 on DESIGN, is an information ratio and is superseded.** Fixed in PR #94; `REPORT_VERSION` 4 → 5, and a v4 point estimate must never be compared with a v5 one. The corrected statistic has not been run on real data, so the direction of the change is unknown and was unknown when the fix was made. Also recording **D-51 step 2**: ALPHA_CHARTER §16.1 is now evaluated once, over the walk-forward splits pooled, under `EvaluationReport.aggregate` (`packages/core/src/research/aggregate.ts`). The charter's decisive falsifier finally has machinery at the scope the charter defines it; **it has not been run on real data**, and the runs that exist remain non-evidential. Two things the work surfaced are the owner's, not Claude Code's: the §16.1/§17 contradiction is now live code rather than a note, and **the registered walk-forward schedule reaches only ~102 monthly-equivalent blocks against §14.3's own minimum of 150**. Earlier the same day, PRs #91–#93 built and repaired Secondary 2. The #91 entry below is kept verbatim: recording PR #91 (`4135453`), which implements ALPHA_CHARTER §11's **Secondary 2** and makes §16.1's decisive second prong computable for the first time. It merged with **three accepted findings open**, by the owner's decision to stop the fix loop; they are listed under *Secondary 2* below and one of them gates the next slice. Earlier the same day: a documentation-only reconciliation, carrying forward a 2026-09-14 pass (`7e30599`) that was written on `claude/busy-meitner-as415q`, never had a PR opened for it, and so never reached `main`. Its charter-banner correction had therefore been sitting unmerged for six days while the misleading banner stayed live. This file had drifted badly: it still placed `main` at `03bf580` (twelve releases and roughly sixty PRs behind), still said no image had been published, and still named "no market data has been ingested" as the sole blocker to a first result, two days after the owner ingested Tiingo bars on the Pi and two evaluate runs completed. Those claims are corrected below. Nothing in this pass changes code behaviour, a charter value, or an approval. Prior: 2026-09-12 (D-49 Tiingo corporate actions); 2026-09-08 (Phase 5 decision gate merged as PR #35; owner signed the charter + `risk.yaml`, D-48).

**Standing lesson from this drift.** `STATE.md` and `HANDOFF.md` are the first two files a fresh session reads, so a stale claim here does not sit quietly in a document — it becomes the next session's premise. The specific failure mode to avoid: the 0.2.0 charter carried a banner reading `DRAFT — AWAITING OWNER SIGNATURE` directly above an approval block the owner had signed two days earlier. A session trusting the banner would have concluded the strategy was unregistrable and gone looking for work that was already done.

## Product state

| Item | Value |
|---|---|
| Phase | Discovery through Phase 3 merged to `main`. Phase 4's four deterministic engines (factor classifier, halt-state machine, risk-limit engine, compliance engine) and Phase 5's decision gate (PR #35) merged. **D-53 slices 1, 2a, 2b, 3a-1, 3a-2 decoder and 3a-2 fetch are all merged** (PRs #81, #82, #83, #85, #86, #87), plus **PRs #91-#95** on 2026-09-20/21 (Secondary 2 and its repairs, D-51 step 2's aggregate §16.1 verdict, and the test-suite headroom pass). `main` is at `cfef173`. The machinery is complete through the shadow rung; **what does not exist is evidence** — see the Registered experiments row |
| Application code | `packages/shared`, `packages/core` (Phase 0 foundation, Phase 1 data/market/universe/research, Phase 2 strategy/research), `packages/broker-gateway` |
| Tests | **918 passing** (unit 851, policy 41, temporal 26; 77 files) - `cfef173` plus this branch, each project count verified by a separate run on 2026-09-21 rather than quoted. The whole `npm run check` set is green: `eslint .`, all five typecheck projects, the three vitest projects, `check:identity`, and `check:secrets` (303 tracked files). Counts in this row have been stale three times; re-run rather than trusting it |
| Live trading | Absent by construction. Config loader and gateway both refuse `LIVE_MANUAL` and `LIVE_LIMITED`; CI asserts the image refuses them too |
| Broker credentials | None exist anywhere in this project. Only the synthetic broker adapter exists |
| Runtime LLM | **Phase 3 machinery merged to `main`** (PR #22, #23, #25): provider-agnostic analyst pipeline + safety surface, the Anthropic adapter behind the single egress module, call archiving/budget persistence, the `research analyst` CLI, and status-page visibility. **No live call has been made** and the one CR-12/CR-13 verification run is still pending — but it is no longer blocked: since D-52, `secrets.env` gives `ANTHROPIC_API_KEY` a route into the container that umbrelOS 1.x's missing `.env` injection previously denied it. The `etf-trend-vol` charter declares no LLM in the signal and only two arms, so nothing wires the analyst into a decision |
| Data ingestion | **All four adapters live-verified 2026-09-07.** FRED (16,880 obs / 5,103 vintages), SEC EDGAR (1,590 obs), CFTC COT (34 obs), Alpaca IEX bars (340 obs / 170 sessions). **Two of the four failed on their first real request** - FRED on CR-28 and CR-29, SEC on CR-30 - all fixed and tested. CFTC and Alpaca passed first time. No production ingest schedule is wired yet (Phase 5). **Tiingo EOD bars** (`tiingo.eod.bars.1d`) were added for the deeper 2007+ history the design window needs (Alpaca IEX starts ~2018); the owner ingested them on the Pi (96,288 bars, 2000-2026). **Since 0.1.11 the refresh is unattended:** an opt-in nightly incremental ingest runs in the `serve` scheduler after each session close, cursored per symbol, enabled by `BLACKGOLD_AUTO_INGEST_CHARTER` (settable from `secrets.env` as of 0.1.12, because umbrelOS 1.x never injects the app-data `.env`). 0.1.11 also fixed a silent total-return double-count when a reconciled and a single-source action for the same dividend both survived a read. **D-49 (2026-09-12):** `ingest tiingo-actions` extracts corporate actions (`divCash`/`splitFactor`) from the same Tiingo feed, flagged `UNVERIFIED_SINGLE_SOURCE` — usable for research/decisions, **never promotion evidence**; the reconciled ≥2-source vendored file stays the only promotion-eligible corporate-action source |
| First Alpha Charter | `strategies/etf-trend-vol/charter.yaml` is **APPROVED at `charter_version 0.2.0`** — owner-signed 2026-09-12 against `code_commit 115dae4`, `approval_ref docs/DECISIONS.md#D-50` (three commits from the owner's own GitHub account: `06515d7`, `6019e08`, `2a960e5`). 0.2.0 adopted Tiingo EOD bars in place of Alpaca IEX, which is a data-transform change and therefore a new strategy version: **it inherits none of 0.1.0's evidence.** 0.1.0 was signed 2026-09-08 against `474d0dc` (D-48/D-39; XLE excluded, 12-ETF universe). `charter show` reports `registrable: true`, `reasons: []`, `charterHash sha256:5c7f94da…`; D-32 (book-slot priority) is owner-confirmed. The §8 two-independent-implementations acceptance test remains a separate open charter-quality criterion (D-48) |
| Registered experiments | **None. No experiment has been registered and the holdout has never been opened.** Two `research evaluate` runs were made on the Pi on 2026-09-13 (charter 0.2.0, `tiingo.eod.bars.1d`, DESIGN and RECENT splits) to exercise the evaluation machinery end to end. Both returned `citableAsEvidence: false` with `promotionBlockingCodes: [UNVERIFIED_SINGLE_SOURCE]`, and **neither is evidence for or against the strategy** — see `docs/analysis/2026-09-13-etf-trend-vol-machinery-check.md`, which says so in its own header. They raised D-51 (the primary promotion metric may be measuring the wrong thing for a tail-risk strategy) |
| Umbrel manifests | Written (`umbrel-app-store.yml`, `blackgold-trading/`), identity-checked, and **installed and running on the Pi** — the "not yet installed anywhere" this row used to carry is long superseded. The install is on umbrelOS 1.x under the legacy-compat shim, which does not inject the app-data `.env` into the container; that platform gap is the whole reason D-52's `secrets.env` fallback exists |
| GHCR image | **Published and in use.** Releases through **0.1.12** (2026-09-13) are out, multi-arch and digest-pinned, cut by dispatching `release.yml` with the bare version (D-38). The app is installed and running on the Pi (the owner ingested 96,288 Tiingo bars there, and D-52 exists because of how that container loads credentials), from which it follows that the first-publish GHCR visibility flip was made — recorded as a sound inference, not a checked fact; confirm against the package settings page if it ever matters. This row previously said "None published yet", which was the most misleading line in this file |

## Repository state

| Item | Value |
|---|---|
| Remote | `https://github.com/mherman1990/BlackGold` (public) |
| `main` | At `cfef173` (PR #95, test-suite headroom; PR #94, D-51 step 2, merged just before it). Matt must still set `main` as the default branch and apply protection in GitHub settings — still not done as of 2026-09-20 |
| Phase 1 PR | [PR #4](https://github.com/mherman1990/BlackGold/pull/4) merged to `main` 2026-09-07 12:56:35Z |
| Phase 2 PRs | [PR #5](https://github.com/mherman1990/BlackGold/pull/5) merged 12:56:48Z into `claude/phase-01-research-kernel`, which PR #4 had already merged forward, so it never reached `main` (D-36). [PR #6](https://github.com/mherman1990/BlackGold/pull/6) carried the same tree to `main` and merged 13:24Z |
| Phase 0 completion PR | [PR #8](https://github.com/mherman1990/BlackGold/pull/8) (release prep) merged 2026-09-07. [PR #9](https://github.com/mherman1990/BlackGold/pull/9) merged 2026-09-07 as `03bf580`: the scheduled daily ledger seal, the three guards that make automatic sealing safe, the ingest event-stamp fix, and the suite-wide test timeout |
| Working branch | Per-task `claude/*` branches off a fresh `origin/main`, one bounded PR each. This 2026-09-20 pass runs on `claude/black-gold-roadmap-bjekzh` |
| Stale branches | **79 `claude/*` branches remain on the remote**, not the three this row used to name. Verified 2026-09-20: **39 are strict ancestors of `origin/main`** and are unambiguously safe to delete. The other 40 predate PR merges that rewrote SHAs, so neither ancestry, `git cherry`, nor commit-subject matching settles them — each needs a content check nobody has done, and this row no longer claims otherwise. Two are known to carry work that never reached `main`: `claude/busy-meitner-as415q` (`7e30599`, the 2026-09-14 docs reconciliation — **carried into `main` by the PR that wrote this line**) and `claude/pin-0.1.9-digest` (moot; the compose pins 0.1.12). Claude Code cannot delete refs (HTTP 403), so they accumulate one per PR and nobody prunes them — which is what made the D-36 mis-merge easy to repeat |
| Unmerged work on a branch | **Resolved.** The stranded slice-2a commit this row used to name (`199e25e` on `claude/phase-05-shadow-decision-job`) merged as PR #82. The one remaining instance was `7e30599` on `claude/busy-meitner-as415q` — a whole docs-reconciliation pass, including a charter-banner correction, that sat unmerged for six days because no PR was opened for it; the 2026-09-20 pass cherry-picked it forward. **The lesson is the pattern, not the commit:** work that is committed and pushed but never given a PR is invisible to every later session, because `STATE.md` is where sessions look and an un-PR'd branch never updates it |
| Branch protection | Not configured (needs Matt in GitHub UI) |
| **Claude Code git permissions** | This session may commit, push to its own `claude/*` branch, open, update and merge PRs. It **cannot create tag refs or delete refs**: `git push origin v0.1.0` and `git push origin --delete <branch>` both return HTTP 403 from GitHub on every attempt, while ordinary branch pushes to the same repo succeed and the agent proxy reports healthy with no relay failures. Not a transient error and not worth retrying. **Worked around for releases** by D-38: `release.yml` accepts a `workflow_dispatch` version and creates the tag itself after checks pass, so the release chain no longer needs Matt. Ref *deletion* has no workaround, so merged branches still do |
| CI | `.github/workflows/ci.yml` (checks plus multi-arch image build) has gated every PR through #87. `testTimeout` is 30 s suite-wide after two runs disagreed on identical commits; see `HANDOFF.md` §7 |

## Secondary 2 and section 16.1's second prong (PR #91, 2026-09-20)

§16.1's decisive falsifier is a **conjunction**: reject only if the strategy fails the primary metric **and** fails to beat Secondary 2. Until PR #91 the second prong could not be evaluated at all, because §11's Secondary 2 — *"VTI scaled to a 10% ex-ante volatility target with the same 63-day estimator, remainder in BIL"* — did not exist in code. What existed was an average-exposure approximation, renamed but not repaired by PR #90.

**What now exists.** Secondary 2 is built as its own total-return index (`packages/core/src/research/benchmarks.ts`), not as a weight fed into `blendSeries`. Each rebalance carries the **instant** `simulateFill` would have acquired it — the decision close at `execution_delay_bars = 0`, otherwise the open of the first tradable bar, counted on the equity leg's **own bars** rather than the session calendar. `k = min(1, target / sigma_primary)` reads the covariance diagonal the strategy already sizes with, so the estimator is literally the registered one and B1's weights are unchanged. Re-scaling is **weekly, at the strategy's decision instants** (owner's call; §11 states no cadence). The prong is measured as **excess return** (owner's reading of §13), as a `Dec`.

**It fails closed.** When the comparator cannot be built exactly the prong is withheld, the arm publishes as `SECONDARY_2_VOL_TARGET_PRIMARY__INEXACT`, `decisiveRejection` reports **unknown** rather than a verdict, and a result carrying no withhold field at all is treated as withheld. Five conditions set that flag: an unplaceable rebalance, an unusable open, a window whose endpoints differ from the run's, a step collapsing daily rebalances because an expected session is missing from either or both legs (exempt when the **pre-open** weight is 0 or 1), and a primary volatility lost after a target exists. Approximating a decisive comparator has no safe direction — under-crediting it makes the strategy easier to promote, over-crediting harder, and which one a given error causes depends on signs you do not control.

**The enumeration is presumed incomplete.** It reached five conditions by enumeration rather than construction, each added because the previous set was incomplete. Treat a sixth as likely.

### The ex-date defect is fixed; the gate is lifted

`legSplit` decomposes a rebalance session into **three** parts, not two: each leg's price move to the open, the **closing session's** distribution credited to the pre-open holder as cash, and the open-to-close price move at the new weight. An ex-date *earlier* in a stretched interval is not cash — the leg's index reinvested it at its own close and it has been compounding since — so the overnight factor carries that growth straight from `trIndex` and applies only the closing session's distribution on top. Adjacent sessions need no special case: the carry is then exactly 1.

Three earlier versions failed in three different ways, all from trying to make the split two factors that multiply back to the leg's own total-return step. They cannot: **the leg's index reinvests its distribution at the close, while a portfolio rebalanced at the open allocates that cash at the open.** Codex's counterexample is the clearest statement — `prevClose 100, open 90, close 100, dist 10`, rebalancing from weight 1 to 0: the holder sells at 90 and keeps the 10, so the portfolio is flat, and the residual form said −1%.

> **Owner reading, not a code choice.** §11 does not say when ex-date income is reallocated. The implementation allocates it **at the open**, with the rebalance, because that is when the portfolio's composition changes. The alternative is the index's close-reinvestment convention. The two differ only on a session that is both ex-dividend and a rebalance. Same shape as the weekly re-scaling cadence — confirm or overrule before the number is treated as decisive.

`SECONDARY_2_KNOWN_DEFECT` and the unconditional withhold are gone with the defect. `primaryVersusSecondary2` is emitted again whenever the comparator placed exactly, so **D-51 step 2 is unblocked**.

### Still open from #91 and #92

1. **(P2) The documented gate was broader than the implemented withhold** — that text is now retired with the gate, but the underlying question survives: the `__INEXACT` arm still publishes `totalReturn` next to `B1_DETERMINISTIC.totalReturn`, so subtracting two published fields reconstructs a withheld prong. Decide whether an inexact comparator should be published at all.
2. **(P2) `EVALUATION_VERSION`, `BACKTEST_VERSION`, `REPORT_VERSION`** are now 3, 4 and 4 — bumped here, closing the deferred finding.
3. **(P2) The `min(1, target / sigma)` test** now recomputes the volatility through `computeFeatures` and asserts the weight to twelve places, with both branches of the `min` exercised. Closed.

### Standing lesson: seven tests certified behaviour they could not detect

The most useful thing this PR produced. Two had expected values reachable by more than one behaviour — the market fixture set `open == close` on every bar, and the look-ahead test used a rebalance session whose move was entirely overnight, so a wrongly split session landed on the same index level as a correct one. Two had fixture parameters that made the guarded branch unreachable: the default primary's volatility never exceeded the 10% target, so `k` pinned at 1 and every collapse was genuinely exempt. One never constrained the quantities it was named for at all.

**Mutation-check any test guarding a financial invariant: delete the behaviour and confirm the test fails.** Four of the eleven review rounds found a defect introduced by the previous round's fix, and in three of those the guarding test stayed green.

## The primary metric was an information ratio (PR #94, 2026-09-20)

ALPHA_CHARTER §13 registers the "difference in after-cost annualized **Sharpe ratio** between the strategy and VTI total return", and `pass_fail.primary_metric` is named `net_sharpe_difference_vs_primary_benchmark`. `buildResultReport` bootstrapped `annualizedSharpe(candidate − benchmark)` — the Sharpe of the difference, which is the **information ratio**. `informationRatio(strategy, benchmark)` in `benchmarks.ts` is defined as `sharpe(strategy, benchmark)`, the identical construction, so `armMetrics` was already publishing that number under its honest name beside the promotion gate that was the same number. Nothing in the suite pinned the statistic; the report tests checked the interval's shape and the threshold.

The two disagree in magnitude **and in sign** whenever the legs differ in volatility or are imperfectly correlated — the premise of a volatility-targeted strategy. Now both legs are taken in excess of cash, their Sharpe ratios are computed separately, and the difference is bootstrapped with `stationaryBootstrapPaired`: one draw of block indices applied to both legs, so the strategy and benchmark stay paired session by session. `REPORT_VERSION` 4 → 5.

**Read every recorded primary-metric number as an information ratio.** The −0.19 on DESIGN, and the 2026-09-13 machinery-check numbers, are all superseded. The corrected statistic has run on synthetic fixtures only, so which way it moves is unknown — and was unknown when the change was made, which is what distinguishes implementing the registered metric from choosing one after seeing a result.

**The first prong is now tri-state.** §13's metric also carries "a deflated-Sharpe adjustment for the registered trial count", and §15 fixes it at N = 72 trials with the observed cross-trial variance — neither of which one evaluation run produces. An undeflated *pass* is therefore not a registered pass and is **withheld** (`passes: undefined`) rather than annotated; an undeflated *failure* is sound, because the adjustment can only add a hurdle, so `REJECT` stays reachable. §16.1 is evaluated in three-valued logic: either prong passing gives owner review even when the other is unknown, both known failures give rejection, anything else is undetermined.

**Two tests survived the first mutation pass, and both were proving the wrong thing** — a paired-bootstrap test that compared against the function under test, and a hash test whose fixtures differed in a second field. Both replaced; nine mutations, no survivors. A third fixture defect surfaced: `research-report.test.ts` built its benchmark and cash series at **constant** growth, i.e. zero volatility, making every Sharpe assertion over them vacuous.

## Section 16.1 at the aggregate scope (D-51 step 2, 2026-09-20)

`packages/core/src/research/aggregate.ts` is the scope §16.1 was always defined at and the code never had. `runEvaluation` collects each walk-forward split's Sharpe input legs and its arm/benchmark total returns, and `aggregateWalkForward` returns **one** verdict — `REJECT | OWNER_REVIEW | UNMEASURED` — under `EvaluationReport.aggregate`. `EVALUATION_VERSION` 3 → 4. There is still **no** per-split §16.1 outcome; §13 scopes the primary metric's pass rule and §16.1 scopes the falsifier to the same aggregate set, and emitting either per split was the defect PR #91 fixed.

**How each prong pools.** `sharpeInputSeries` yields **two** legs per session — the candidate's and the benchmark's daily excess over **cash** — and the first prong concatenates **both** legs across splits in session order, then runs `stationaryBootstrapPaired` once (21 sessions, 90%) with `annualizedSharpeDifference` as the statistic: one draw of block indices applied to both legs, so the pairing survives. It is a **difference of Sharpe ratios**, not the Sharpe of a difference. Pooling the *input*, not averaging per-split *results*, is what makes it the same statistic at a wider scope. The second **chain-links** the per-split total returns for the candidate and for Secondary 2 independently, because each window is backtested from cash, and takes the difference.

**Four fail-closed rules, each there to stop a rejection being manufactured.** Walk-forward splits only — anything else throws. The pool must be the **whole** schedule, or the verdict is `UNMEASURED`; and **both** outcomes are withheld from a partial pool, not just the rejection, because "goes to owner review" is equally a claim about a set that was not measured. Both prongs must be measured — one split without a usable Secondary 2 leaves the aggregate prong `undefined`, since absence is not failure. A short series is **not** padded the way `buildResultReport` pads the per-split diagnostic.

### Three limits that ride on every verdict

1. **The registered deflated-Sharpe adjustment is not applied.** §13 registers one for the trial count. `deflatedSharpe` exists but needs the cross-trial Sharpe dispersion of the full 72-member grid, which one run does not produce. Deflation can only lower a Sharpe, so the first prong is **easier** to pass than the registered statistic — the omission biases §16.1 toward owner review and away from rejection.
2. **Each window restarts from cash.** At every boundary the strategy sits flat until its first fill while the benchmark is fully invested, so the pooled excess carries a warm-up drag and a round of re-entry cost. Bootstrap blocks can straddle a boundary; concatenation is §13's literal reading, and a within-split bootstrap would be an unregistered estimator.
3. **The schedule cannot reach the charter's own minimum.** `splitPlan` on 0.2.0 yields **9** walk-forward splits tiling **2010-06-28 → 2018-12-31** — about 2,150 sessions, roughly **102** monthly-equivalent blocks — against §14.3's minimum of **150**. §14.3's "155 in design, 72 in holdout" counts the design window and the sealed holdout, neither of which is the walk-forward out-of-sample set. §16.1 states no such precondition, so the verdict is computed and the shortfall reported.

**A data gap withholds the first prong, both ways (owner's call, 2026-09-20).** A gap in a **held risk ETF** cannot be seen from the level series: `dailyNavSeries` marks the missing holding at its previous close, so the candidate arm has a point on every session while that holding's multi-day move lands in one later return. `GAP` and `STALE_BAR` are `promotionEvidenceAllowed: true`, so the run is otherwise citable. Any pooled split reporting such a session sets `passes: undefined`. **The signal is computed from the backtest's own bars, not from `bt.labels`** — the labels cannot carry it: `loadExecutionSeries` keeps only corporate-action codes and filters even those through `blocksPromotionEvidence` (which excludes GAP and STALE_BAR by definition), `STALE_BAR` never becomes a series label, and `RawSeries` derives GAP only between the first and last loaded bar. A first attempt keyed the withhold off the labels and would have been inert. `BACKTEST_VERSION` 4 → 5 for the new `navDistortingSessions` field. Unlike the deflated-Sharpe withhold this covers failures too, because a gap's distortion has no fixed sign — so a failing threshold test is as untrustworthy as a passing one, and withholding only the pass would keep `REJECT` reachable on a number nobody can vouch for. Expect `UNMEASURED` on gappy data; it lifts when the data is clean, or when constituent gap sessions are plumbed through `BacktestResult`.

### Two things now waiting on the owner

- **§16.1 and §17 contradict each other, and the code now says so out loud.** When the primary metric fails and Secondary 2 passes, the verdict is `OWNER_REVIEW` per §16.1 and `charterConflict` quotes §17's "REJECTED, never to ACTIVE". Both are in the signed charter. Settle it **before** a real run produces that case.
- **Either the walk-forward schedule or §14.3's minimum is wrong for this charter.** Both are charter values, so both are the owner's.

Secondary 2's two open readings — the weekly re-scaling cadence and the ex-date reinvestment convention — are attached to any verdict resting on the second prong (`SECONDARY_2_OPEN_READINGS`). **Until they are settled, a §16.1 rejection from this code is provisional.**

**Tests: sixteen mutations, no survivors.** Every guard above was verified by deleting or inverting it and confirming a test fails. The chain-link fixture is the clearest: candidate +50%/+50% against Secondary 2 +100%/+10% gives **+0.05 linked** and **−0.10 summed**, so a summing implementation rejects exactly where a linking one routes to owner review.

## Decisions

All Discovery recommendations accepted by Matt on 2026-09-06 (see the header of `docs/DECISIONS.md`). Phase 2 added D-32 to D-36; D-37 grants Claude Code standing git and GitHub authority, with the carve-outs in `CLAUDE.md` that no autonomy reaches. **D-49 (Accepted 2026-09-12 by Matt, "Item 3, use A")** amends D-29 to permit automated single-source corporate actions from Tiingo, flagged `UNVERIFIED_SINGLE_SOURCE` (research-only); the multi-source reconciled vendored file remains the only promotion-eligible path. **D-50 (Proposed 2026-09-12; owner signs the 0.2.0 charter in PR #57)** adopts Tiingo (`tiingo.eod.bars.1d`) as the strategy's default market-data source, revising OD-4 and cutting `charter_version` 0.2.0 — a new strategy version that does not inherit 0.1.0's evidence. This PR flips `DEFAULT_BARS_SOURCE_ID` to Tiingo and adds the `tiingo.` 15-minute market processing delay **atomically** with the signature (Codex P1: no window where a 0.2.0-labelled run reads the old Alpaca feed). On merge, the strategy's default source is Tiingo.

**D-50 is resolved — owner-signed 2026-09-12,** so the sentence above it (written while D-50 was still Proposed) is superseded: the strategy's default market-data source *is* Tiingo, and the running strategy version is 0.2.0. **Still Proposed and awaiting Matt:** D-40 (Granary owns the household layer — Proposed since 2026-09-07 and blocking any Phase 4 household work), D-51 (the primary promotion metric), D-52 (the file-based secrets fallback — note its code shipped in 0.1.11/0.1.12 under D-37 merge authority, which is not the owner acceptance that decision names; the gap is recorded in D-52 rather than closed), and D-53 (the paper/shadow decomposition).

**D-32 is resolved — owner-confirmed 2026-09-08.** The prose charter's entry rule and its hysteresis hold rule can name six ETFs for a five-slot book, and section 9 did not say which yields. The code resolves it in favour of the incumbent, because resolving by rank alone would make the hysteresis band dead code whenever five names are eligible. Matt confirmed the incumbent-priority reading by adding a `Book-slot priority` row to section 8 of `ALPHA_CHARTER.md` (commit `13d637a`); it matches `candidates.ts`, so no code change was needed.

Open facts still owed by Matt: whether the sleeve account exists and its type (D-12), backup destination host (D-16), Phase 6 written blast-radius acceptance (D-13), and the port-collision check on the Pi before first install (D-04). The charter's four open decisions are resolved (D-39) and its approval block is signed (D-48).

## External context: Granary (separate product, added 2026-09-07)

Matt is building the household / personal-finance / capital-allocation layer as a **separate** product, **Granary** (`mherman1990/Granary`, Umbrel app `granary`, port 3000), which sits *above* Black Gold and will eventually read Black Gold data. This changes Black Gold's Phase 4 scope: Black Gold does **not** become the household book of record and does **not** take any dependency on Granary. Integration is one-way (Granary reads Black Gold, never the reverse) and Black Gold's existing boundaries (no inbound nonpublic/household connector, no household dollars to a model, no money movement) are the safety guarantee. Recorded as **D-40 (Proposed)**; it blocks no current work and awaits Matt's acceptance before any Phase 4 household work. Do not rebuild the household layer inside this repository. The read-only export Granary would consume is designed (dollar-free by construction) in `docs/GRANARY_EXPORT_CONTRACT.md` (Proposed): Black Gold exports weights and states, never dollars or accounts, and Granary supplies the dollar denominator. Building it is a Phase 4/5 PR, authorized on its own.

## Phase 3 status (2026-09-07)

Phase 3 (bounded runtime-LLM analyst overlay) was authorized by Matt (D-41) and is **merged to `main`** ([PR #22](https://github.com/mherman1990/BlackGold/pull/22)). The **provider-agnostic pipeline and its full safety surface are built and tested** with a deterministic stub adapter (no credentials, no network): `ResearchAssessment` schema + validator, sealed evidence packet + serialization guard, `ModelAdapter` + `runAssessment` orchestration (budgets, deadline, retries, circuit breaker, no silent fallback, abstention), B0/B1/C1/D1 overlay primitives with contamination labels, the model capability manifest + fail-closed resolver, the adversarial corpus, the T-05 no-LLM-in-sizing CI gate, and the locked ablation plan (`docs/PHASE3_ABLATION_PLAN.md`). ~55 new tests; `npm run check` green.

The **real Anthropic adapter is now built** in a follow-up PR (D-42, branch `claude/phase-03-provider-adapter`): `AnthropicAdapter` over the single model-egress module `packages/core/src/model/provider-http.ts` (raw `fetch`, no SDK), with `live-disabled.test.ts` updated to allow `api.anthropic.com` + the POST in exactly that module while trading hosts stay forbidden and `data/http.ts` stays POST-free; the compose passes `ANTHROPIC_API_KEY` through (empty default). Request/response mapping is tested against a mock transport; no key is in the repo (Matt rotated the one pasted in chat).

**Still deferred, by design — see `docs/PHASE3_REQUIREMENTS_MATRIX.md`:** one live verification run against the real API on the Pi (CR-12/CR-13 — exact wire shape, latency, cache); persistence of the model-call record and running budget; and wiring C1/D1 into the decision loop (prospective, Phase 5). No live trading mode, broker credential, or trading-host egress is added. No LLM output can set a size, choose an account, or form an order.

**Phase 3 completion (merged to `main` at `9238e94`, [PR #25](https://github.com/mherman1990/BlackGold/pull/25), 2026-09-07):** call archiving + budget persistence (migration `0007` `model_calls`, `research/model-call-log.ts`), the analyst wiring (`analyst/run-analyst.ts` + a `research analyst` CLI command reading `ANTHROPIC_API_KEY` in `config/load.ts` only), and operator visibility of the archived-call count on the read-only status page (`EvidenceStatus.modelCalls`, count-only within the page's A3/T-22 constraints) are built and tested with a stub adapter. This closes the "archive everything" criterion. Unlike PR #9, this PR got an independent pass: the Codex security review completed on `ce4c41c` with no findings. **Still open:** the live CR-12/CR-13 run (needs a key on the Pi) and C1/D1 backtest wiring (Phase 5, review-gated). The `research analyst` command fails closed without a key and makes no call.

## Phase 4 status (2026-09-07)

Phase 4 (household-minimum, compliance, restricted list, portfolio construction, sizing, risk engine) was authorized by Matt (D-43) and is being built as a sequence of bounded PRs, unblocked pieces first, because several deliverables depend on `risk.yaml` approval and the sleeve account (D-12).

**First Phase 4 PR — the deterministic factor classifier (branch `claude/phase-04-factor-classifier`):** a `factors` block in the charter schema (`taxonomy` + per-symbol `assignments`, hash-covered) is the code-side authority the Analyst's `factorsTouched` is checked against; `packages/core/src/strategy/factors.ts` classifies a candidate and the `research analyst` CLI now derives the factor set from the charter and refuses an unclassified candidate (unknown classification blocks new risk), replacing the `--factors` flag. The etf-trend-vol charter carries a **conservative starting map** that is hash-covered, so it was frozen into the charter Matt signed (D-48); the factor values are now part of the approved charter, and revising any tag is a new charter version at DRAFT (his act).

**Second Phase 4 PR — the risk halt-state machine (D-44, branch `claude/phase-04-risk-halt-engine`):** `packages/core/src/risk/halt.ts` (`evaluateHaltState`) is the deterministic risk safety core. It returns `NORMAL`, `HALT_NEW_RISK`, or `HOLD_ONLY` — never `EMERGENCY_FLATTEN_AUTHORIZED` — from sleeve NAV/HWM/session-start NAV, staleness/incident signals, and the `risk.yaml` thresholds: drawdown, daily loss, stale input, expired auth, unknown state, and severe incident all fail closed to `HALT_NEW_RISK` (or `HOLD_ONLY` at the deeper drawdown); automatic flatten never happens; escalation is automatic and relaxation needs an owner re-arm that an active fault still overrides. Sizing (`strategy/construct.ts`) and the risk-policy schema/defaults (`RiskConfigSchema`, `config/examples/risk.yaml`, D-15) already existed, so this closes the "risk engine with halt states" gap. **`risk.yaml` is now owner-approved:** Matt signed the approval block himself on 2026-09-08 (`approvedBy "Matt Herman"`), resolving OD-3 (D-48). Claude Code did not perform the signature — approving the defaults is one of the acts no autonomy reaches. The engine consumes an approved policy now, but still forms no order and runs nothing for real: that path additionally needs ingested data, the registry call, and a `LIVE_AUTHORIZATION`.

**Third Phase 4 PR — the risk-limit/caps engine (D-45, branch `claude/phase-04-risk-limits`):** `packages/core/src/risk/limits.ts` (`evaluateRiskLimits`) is the deterministic `RiskEngine` verdict — an independent re-check of a proposed target book against the `risk.yaml` caps and the charter, separate from construction, that admits or rejects with reason codes and never re-sizes. Checks: per-instrument weight, open-position count, gross/net exposure, cash floor, sector concentration, correlated-cluster weight and membership, and the long-only posture; fail-closed on an unclassified holding. Pure (T-05, under `risk/`). 11 tests. **Factor concentration is deferred on a real owner question** (the taxonomy's `market` tag is on every holding, so capping summed `market` weight would cap total exposure — which factor tags are cap-bearing is a policy decision); liquidity, order-level, and per-position-risk-budget limits need order/price/ADV data and are follow-ups.

**Fourth Phase 4 PR — the compliance engine (D-46, branch `claude/phase-04-compliance-restricted`):** `packages/core/src/compliance/engine.ts` (`evaluateCompliance`) is the deterministic compliance verdict — admit/reject a candidate against the restricted list with reason codes, pure. It encodes the D-14 rules: additions immediate, removals wait a cooling period (`pendingRemovals` until `eligibleAt`); blackout windows block only new risk; a stale list fails closed. Themes are checked against supplied exposures. 6 tests. **The restricted-list *content* is Matt's**, not Claude Code's: it encodes his nonpublic professional restrictions (employer, suppliers, themes), so `config/examples/restricted-list.yaml` keeps fake placeholders and populating the real list is the owner's act (like approving `risk.yaml`). ETF **look-through** (deriving an ETF's restricted-theme exposures) is a deferred Phase 4 piece — the engine takes exposures as input.

**Still to come in Phase 4:** ETF look-through + exposure flags, the deferred limit engines (factor concentration once the cap-bearing tags are decided; liquidity/order-level/per-position-risk), and wiring the research/compliance/risk/limit verdicts + construction into the decision loop (Phase 5) — each a further bounded PR. `risk.yaml` (OD-3) is now approved, so the remaining gates are the real restricted list, ingested data, and the sleeve account (D-12) where they depend on them.

## Phase 5 status (2026-09-08)

Phase 5 (composing the deterministic verdicts into one go/no-go and, downstream, the prospective decision loop) was authorized by Matt (D-47) and is being built as bounded PRs, deterministic composition first.

**First Phase 5 PR — the deterministic decision gate (D-47, merged to `main` as PR #35):** `packages/core/src/decision/gate.ts` (`evaluateDecisionGate`) is the single place that composes the three independent Phase 4 verdicts into one go/no-go for **new risk**: new risk may proceed iff the halt state is `NORMAL` **and** the proposed target book respects every risk limit **and** every candidate newly taking risk clears compliance. Fail-closed by construction — any single block makes `newRiskAllowed` false, and the blocking reasons are flattened into `blockedBy` for the decision record. It forms no order and never re-sizes; it decides only whether the deterministically-constructed target may be acted on. Compliance is always evaluated as **new risk** for each candidate (the gate fixes `isNewRisk: true`), because the gate's whole question is whether new exposure may be added. **Compliance coverage is enforced, not trusted** (Codex P1): the gate takes the current book too and derives the holdings taking new/increased risk (`target > current`); an increase with no supplied compliance evaluation fails closed (`MISSING_COMPLIANCE`), while a held-flat or reduced position needs no candidate. Coverage binds each increasing holding to its candidate by canonical key, not by the candidate's alias set (a second Codex P1 on the fix), so one clean candidate can't cover another holding it merely lists as an identifier. Pure and under `decision/`, composing only `risk/` and `compliance/`, so no model output can reach it (T-05). 9 composition tests. The equivalent guards run again in the broker gateway; this is the core-side gate.

**Paper/shadow track started (D-53, owner chose it 2026-09-14): slice 1 is built** — the sealed, timestamp-locked prospective decision record (`decision/decision-record.ts`, migration `0008_decision_records`): one append-only row per (strategy version, arm, decision instant) carrying the deterministic target book (weights as fractions of NAV) + the decision-gate verdict, with no order/broker/account/fill field, a currency/secret redaction guard, and a mode guard admitting SHADOW/PAPER and refusing RESEARCH/BACKTEST (the ladder's "BACKTEST may not write the prospective decision ledger", in code). Immutable per instant. Touches no broker, no scheduler, and does not change `backtest.ts`. **Slice 2a is built** — the pure per-arm target book (`decision/prospective.ts`): B0 passive (100% benchmark) and B1 deterministic (features → candidates → sizing) composing the same leakage-audited primitives the backtest uses; a permanent anti-drift test binds B1's weights to `runBacktest`'s first decision byte-for-byte, so a prospective decision cannot diverge from the backtested one. Reads only point-in-time; forms no order; does not change `backtest.ts`. **Slice 2b is built** — the pure gate-and-seal function (`decision/shadow-decision.ts`): for each arm it wires the slice-2a target book through `evaluateDecisionGate` (B1 runs the full halt + limits + new-risk-compliance gate; B0, the passive comparator, is bound only by the halt state) and seals a slice-1 `ProspectiveDecisionRecord`. Pure — charter, read surface, `RiskConfig`, `RestrictedListConfig`, shadow book state, and the decision/seal instants are all passed in; no config/env/clock/DB read, no order, no broker. Compliance fails closed honestly: ETF theme look-through is not wired yet, so a candidate's `themeExposures` is `undefined`, which blocks B1 new risk (`UNKNOWN_LOOK_THROUGH`) — the sealed target weights (byte-for-byte the backtested ones) are the evidence, and the verdict truthfully says no new exposure cleared. Decide-from-empty each call (no cross-session state yet); does not touch `serve.ts` or `backtest.ts`. **Slice 3a-1 is built** — the pure ETF theme look-through engine (`compliance/look-through.ts`): Matt chose the full ALPHA_CHARTER §2.2 holdings computation (over an interim declared map), so `evaluateLookThrough(holdings, membership, params, now)` sums the DISTINCT restricted-theme issuers' weight in the ETF's published holdings and clears (empty `themeExposures`) at or below the owner-set threshold (§2.2 proposes 10% of NAV), else names the present restricted themes so compliance blocks; unknown (no holdings, or staler than the freshness limit) returns `undefined` → `UNKNOWN_LOOK_THROUGH` (fail closed). `lookThroughResolver` is the `(etf) => themeExposures | undefined` adapter `shadow-decision.ts` consumes. Pure — holdings, issuer→theme membership, threshold, and freshness limit are all passed in (owner compliance content is never authored here); no ingest, no network, no charter change. **Slice 3a-2 decoder is built** — `data/adapters/ssga-holdings.ts`: `decodeSsgaHoldings(bytes, {etf})` turns an SSGA SPDR daily-holdings `.xlsx` (SSGA publishes XLI/XLP only as Excel) into a validated `{ etf, asOf, lines }` with weights converted percent-of-NAV → fraction; decoded with `read-excel-file` (small, ESM-native, zero-CVE, read-only — chosen over SheetJS npm `xlsx`'s unfixed CVEs since the file is untrusted, and over hand-rolling per Matt's "add a small dep" call). Fail-closed via `SchemaDriftError` on any surprise (unreadable, no header, wrong fund, no as-of date, no constituents, weights not ~100%). First `packages/core` runtime dep beyond decimal.js/yaml/zod; `.xlsx` added to `check-secrets` binary-skip. **Slice 3a-2 fetch is built** — `fetchSsgaHoldings` (one request per ETF to `www.ssga.com`, added to the http allowlist; unauthenticated, no credential) stores the raw `.xlsx` and wraps each decoded file as ONE PIT observation under `etf_holdings.ssga.<ETF>`: effectiveAt = the holdings as-of date, availableAt = vintageAt = the **fetch instant** (`ingestedAt`, `AVAILABLE_AT_ESTIMATED`) — not derived from the as-of date, so a corrected workbook re-published under the same as-of date supersedes only from when it was ingested rather than reading back retroactively via `asOf`'s row-id tiebreak (a Codex P1 fix); `etf_holdings.` gets a 15-min processing delay; `ingest ssga-holdings --etfs XLI,XLP` CLI + e2e test. Lands point-in-time data only; forms no decision. Remaining D-53 slices: (3a-3) owner-authored theme-membership config (schema + fake example) + wire the resolver into the shadow decision/serve job; (2c) the mode-gated `after_close` `serve` job — Matt decided 2026-09-14 to **bake the operative `risk.yaml`/`restricted-list.yaml` into the image** (versioned, hashed like `risk_yaml`), and to do look-through before 2c; (3b) counterfactual fills + reconciler; (4) the Alpaca **paper** broker adapter (needs paper keys + the D-12 sleeve decision). Building the machinery does not climb the ladder — Rung-2 evidence still cannot precede the Rung-1 experiment, and the strategy must still pass its own gate (D-51).

**Still to come in Phase 5:** wiring construction + the analyst overlay (C1/D1) into the gate to produce a persisted counterfactual decision ledger; the prospective paper/shadow run (needs paper keys); production ingestion scheduling; the reconciler/steward and incident records; and cost monitoring — each a further bounded PR. `risk.yaml` (OD-3) and the charter signature are both done now, so the remaining gates are the real restricted list, credentials/ingested data, and the sleeve account (D-12) where they depend on them.

## Phase 2 exit criteria

See `docs/PHASE2_REQUIREMENTS_MATRIX.md`. Every criterion verifiable without owner approval is `tested`. One remains `blocked`: "predeclared research tests complete and reported". The reason has **changed** since the matrix was written, and the matrix itself has not been updated — it was "no market data has been ingested", which is no longer true. Data is ingested and the machinery has been exercised end to end; what is missing is that no experiment has been *registered* and no run is citable, because reconciled ≥2-source corporate actions do not exist yet. Still the honest state, for a different reason.

## Phase 1 exit criteria

See `docs/PHASE1_REQUIREMENTS_MATRIX.md`. All five are tested. Not yet exercised: a live ingest against any source, and the Pi resource measurement of an ingest run.

## Phase 0 exit criteria

See `docs/PHASE0_REQUIREMENTS_MATRIX.md`. Everything verifiable in this repository is `tested`. Two criteria need Matt's hardware: containers running on the Pi and on Windows Docker, and measured Pi resource use against `docs/RESOURCE_BUDGET.md`.

**Row 9 was wrong for a day and is now corrected.** It claimed the ledger's *daily* seal was `tested` from 2026-09-06 to 2026-09-07. It was not: `sealDaily()` was reachable only from the `seal` CLI, so a deployed `serve` process registered one job (the heartbeat) and never sealed anything. Found by Codex review on PR #8, fixed in PR #9, and the matrix now records what was overstated and for how long.

The lesson generalises, which is why it is here and not only in the matrix: a row citing unit tests of a *mechanism* is weaker evidence than one citing the *deployed path*. Prefer the latter when marking anything `tested`.

## Capability register status

Verified: 17. Partial: 4. UNVERIFIED: 3 (all Schwab rows, Alpaca duplicate client-order-id semantics, Alpaca IEX entitlement). Umbrel packaging, NYSE calendar, and multi-arch CI facts were verified on 2026-09-06.

## Next authorized action

`docs/analysis/2026-09-20-blocker-queue.md` is the session-resume companion to this section: the same blockers, ordered by leverage, with the work that needs none of them listed alongside.

Four of the five steps this section used to list are done (GHCR publish, D-32, the charter signature, the data credentials). What replaced them is a sharper picture of the one thing standing between this repository and its first citable result.

**The blocker is data curation, not code.** The 2026-09-13 runs came back `citableAsEvidence: false` because of `UNVERIFIED_SINGLE_SOURCE` — and that code is stamped on **corporate actions**, never on bars. The Tiingo actions adapter flags every dividend and split unconditionally (`data/adapters/tiingo-corporate-actions.ts:117`), and the backtest folds any promotion-blocking action flag into the run's labels. So a second *bars* vendor would do nothing for citability. The only promotion-eligible path is the operator-curated file, where the flag clears per entry and only when the entry names ≥2 distinct sources (`data/adapters/corporate-actions.ts:105`). Full reasoning: `docs/analysis/2026-09-13-path-to-citable-evidence.md`.

**Matt's, in rough order of what unblocks the most:**

1. **Curate reconciled corporate actions** for the 12-ETF risk universe + BIL + VTI over the evaluation window (DESIGN is 2007-06-01 → 2018-12-31; RECENT is 2025-01-01 → present; the holdout window is deliberately out of scope). Each action agreed across ≥2 independent public sources — issuer distribution notices and an exchange feed — with both named in `sources`, then `ingest corporate-actions --file <path.json>`. Order of a few hundred quarterly dividend entries plus a handful of structural actions. No code change required. This is the step that makes a citable run possible at all.
2. **Decide D-51** — whether `net_sharpe_difference_vs_primary_benchmark` is the right *primary* promotion metric for a strategy whose stated thesis is drawdown reduction. Worth settling **before** the holdout is opened, because changing it afterwards is a new charter version that inherits nothing. Claude Code may not decide this.
3. **Accept, amend, or reject D-40, D-52 and D-53.** D-40 has been Proposed for a week and blocks Phase 4 household work; D-52's code is already live without the owner act that decision names as acceptance.
4. **Set `main` as the default branch and apply protection**, and prune the stale `claude/*` branches (39 verified safe; see the Stale branches row). While doing it, settle the `v*` tag question `HANDOFF.md` §4 raises: D-37 grants Claude Code tag creation while the credential is refused tag refs, so the register and the enforcement disagree.
5. **Answer the standing open facts:** D-12 (does the sleeve account exist, and its type — gates D-53 slice 4 and Phase 6), D-16 (backup destination), D-04 (port check before install), and the real restricted-list content plus D-14 counsel review.

**Claude Code's, available now without further owner input:** D-53 slice 3a-3's code half (the theme-membership schema + a fake example, and wiring `lookThroughResolver` into the shadow decision path — the owner's real content then drops in with no code change); slice 2c (the mode-gated `after_close` `serve` job, with `risk.yaml`/`restricted-list.yaml` baked into the image per Matt's 2026-09-14 call); slice 3b (counterfactual fills + reconciler + incident records — the largest remaining piece needing nothing from the owner); the deferred liquidity and order-level limit engines; and a bars cross-source verifier (Tiingo vs Alpaca), which is worth real data-integrity confidence but **does not** clear the citability label and must not be presented as if it does. Factor-concentration caps stay blocked on the owner question of which factor tags are cap-bearing, since the `market` tag sits on every holding. Slice 4 needs paper keys and D-12. The live CR-12/CR-13 analyst run is now possible on the Pi, since `secrets.env` (D-52) gives `ANTHROPIC_API_KEY` a route into the container.

**What no amount of autonomy reaches, stated once more because the next steps sit right next to it:** signing a charter, resolving its open decisions, opening the sealed holdout, citing any run as promotion evidence, enabling a live mode, or introducing a broker credential. Matt performed the D-32 confirmation and both charter signatures himself; Claude Code prepared the edits and did not apply them. Signing a charter it wrote, or grading its own results, would make `assertRegistrable` decorative.

## Review coverage, stated plainly

PR #9 changed the ledger's integrity model and **no external review covered it**. Codex was out of usage budget for its code review on every commit of that PR, and a re-requested security review hit the same limit; the only security pass that completed ran on `d9d8054`, which predates the three commits that matter. The corruption path that PR #9 fixes was found by a local review pass, i.e. by Claude Code reviewing Claude Code.

That is a thinner evidence base than the ledger deserves. It did not block the merge (CI green, fail-closed in direction, no order/account/credential/live path touched, and no capital depends on this build), but a fresh pair of eyes on `packages/core/src/ledger/ledger.ts` and the `seal_ledger` job is worth having before any phase writes at volume.

Market data is now ingested (Tiingo bars on the Pi, refreshed nightly since 0.1.11), the charter is signed at 0.2.0, and D-32 is confirmed — so every gate this paragraph used to name is cleared. What a *citable* experiment now waits on is reconciled corporate actions (step 1 above). `HANDOFF.md` section 5 remains the ordered procedure for producing a Phase 2 result, marking which steps are reversible and which one is not.

**Merge order matters in this repository.** Twice a phase PR was merged into a base branch that had already been merged forward, leaving `main` a phase behind (PR #3 for Phase 1, PR #5 for Phase 2). A stacked PR must be retargeted to `main` *before* it is merged, or merged before its base goes in. D-36 records the rule.
